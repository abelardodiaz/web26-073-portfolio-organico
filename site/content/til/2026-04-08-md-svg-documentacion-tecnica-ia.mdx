---
title: "Markdown + SVG: la combinacion que las IAs realmente entienden"
slug: "md-svg-documentacion-tecnica-ia"
category: "tooling"
stack: ["markdown", "svg", "documentacion"]
date: "2026-04-08"
---

## Contexto

Llevaba meses documentando proyectos solo con Markdown. Funciona bien para texto: decisiones, listas de componentes, snippets de codigo. Pero cuando intento explicar una arquitectura de varias capas o un flujo de decision con bifurcaciones, el Markdown se convierte en un muro de texto con flechas ASCII que ni yo entiendo al releerlo.

Un dia probe generar diagramas SVG como complemento al MD. Los puse en la raiz del proyecto junto al documento principal. Lo que no esperaba: cuando otra IA leyo el SVG como contexto, lo entendio perfectamente. No como imagen -- lo parseo como documento estructurado. Entendio las capas, las conexiones, las dependencias. Mejor de lo que yo esperaba.

## Lo que aprendi

La clave es que Markdown y SVG son complementarios, no redundantes. El MD explica el "que" y el "por que" en detalle. El SVG muestra el "como se conecta" de un vistazo.

### Markdown: el manual de referencia

Un documento MD bien escrito le dice a la IA (o al humano) exactamente que hace cada pieza:

```markdown
## Arquitectura del pipeline

El sistema procesa datos en 3 etapas secuenciales:

- **Ingesta** — recibe eventos via webhook, valida schema JSON
- **Transformacion** — normaliza campos, aplica reglas de negocio
- **Salida** — envia a PostgreSQL + notifica via Redis pub/sub

La ingesta usa `fastapi` con validacion Pydantic.
El rate limit es 100 req/s por tenant.
```

Y asi se ve renderizado -- es lo que estas leyendo ahora mismo. Markdown es texto plano que cualquier editor, celular o terminal puede abrir. No necesitas software especial. Git lo diffea linea por linea. Una IA lo parsea sin problema.

### SVG: el mapa visual

Ahora el mismo pipeline pero como diagrama. Primero el codigo:

```xml
<svg viewBox="0 0 520 80" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a" viewBox="0 0 10 10" refX="8" refY="5"
      markerWidth="6" markerHeight="6" orient="auto">
      <path d="M2 2L8 5L2 8" fill="none"
        stroke="#888" stroke-width="1.5"/>
    </marker>
  </defs>
  <rect x="10" y="18" width="130" height="44"
    rx="6" fill="#E1F5EE" stroke="#0F6E56"/>
  <text x="75" y="44" text-anchor="middle"
    font-family="sans-serif" font-size="13"
    fill="#085041">Ingesta</text>
  <rect x="195" y="18" width="130" height="44"
    rx="6" fill="#E6F1FB" stroke="#185FA5"/>
  <text x="260" y="44" text-anchor="middle"
    font-family="sans-serif" font-size="13"
    fill="#0C447C">Transformacion</text>
  <rect x="380" y="18" width="130" height="44"
    rx="6" fill="#EEEDFE" stroke="#534AB7"/>
  <text x="445" y="44" text-anchor="middle"
    font-family="sans-serif" font-size="13"
    fill="#3C3489">Salida</text>
  <line x1="140" y1="40" x2="193" y2="40"
    stroke="#888" stroke-width="1.5"
    marker-end="url(#a)"/>
  <line x1="325" y1="40" x2="378" y2="40"
    stroke="#888" stroke-width="1.5"
    marker-end="url(#a)"/>
</svg>
```

Y aqui el mismo SVG renderizado en tu navegador:

<div style={{border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: '1.5rem', margin: '1.5em 0', background: 'var(--card)'}}>
<svg viewBox="0 0 520 80" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
      <path d="M2 2L8 5L2 8" fill="none" stroke="#888" strokeWidth="1.5"/>
    </marker>
  </defs>
  <rect x="10" y="18" width="130" height="44" rx="6" fill="#E1F5EE" stroke="#0F6E56"/>
  <text x="75" y="44" textAnchor="middle" fontFamily="sans-serif" fontSize="13" fill="#085041">Ingesta</text>
  <rect x="195" y="18" width="130" height="44" rx="6" fill="#E6F1FB" stroke="#185FA5"/>
  <text x="260" y="44" textAnchor="middle" fontFamily="sans-serif" fontSize="13" fill="#0C447C">Transformacion</text>
  <rect x="380" y="18" width="130" height="44" rx="6" fill="#EEEDFE" stroke="#534AB7"/>
  <text x="445" y="44" textAnchor="middle" fontFamily="sans-serif" fontSize="13" fill="#3C3489">Salida</text>
  <line x1="140" y1="40" x2="193" y2="40" stroke="#888" strokeWidth="1.5" markerEnd="url(#a)"/>
  <line x1="325" y1="40" x2="378" y2="40" stroke="#888" strokeWidth="1.5" markerEnd="url(#a)"/>
</svg>
</div>

Eso es un archivo de texto plano. Pesa menos que una imagen PNG. Git lo diffea. Un navegador lo renderiza bonito. Y una IA lo lee como documento estructurado: ve tres nodos, dos conexiones, una secuencia de izquierda a derecha.

### Por que funciona tan bien para IAs

Un SVG no es una imagen opaca como un PNG o JPG. Es XML con semantica:

- Cada `<rect>` es un nodo con posicion y color
- Cada `<text>` tiene contenido legible
- Cada `<line>` con `marker-end` es una conexion dirigida
- Los atributos `<title>` y `<desc>` agregan contexto que la IA lee aunque el humano no los vea

Cuando una IA recibe un SVG como contexto, no lo "ve" como imagen -- lo parsea como estructura de datos. Entiende jerarquias, flujos, dependencias. Y lo hace mejor que con diagramas ASCII o listas de bullets intentando describir relaciones espaciales.

### La receta practica

Para documentar un proyecto para que tanto humanos como IAs lo entiendan rapido:

1. Un **MD** en la raiz con: que es, por que existe, que decide cada componente, que stack usa
2. Un **SVG** por cada concepto que tenga relaciones espaciales: capas de arquitectura, flujos de decision, fases de proyecto
3. Ambos en la raiz del repo, ambos versionados en git, ambos diffeables

Los dos formatos son abiertos, pesan kilobytes, y se leen en cualquier computadora o celular con un editor de texto. El SVG ademas se renderiza bonito en cualquier navegador. No necesitas Figma, Lucidchart ni Mermaid -- un archivo de texto con angulos y coordenadas.

Lo que me sorprendio es que no habia considerado los SVGs como documentacion. Siempre los vi como "imagenes vectoriales". Pero resulta que son documentos estructurados que comunican relaciones mejor que cualquier lista de bullets. Y las IAs los entienden de primera.
